{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} | Connexion{% endblock %}

{% block body %}
    <div {{ stimulus_controller('vue-login-form') }}></div>
{% endblock %}

{% block javascripts %}
    <script>
        window.lastUsername = "{{ last_username }}"
        {% if error %}
        window.error = "{{ error.messageKey|trans(error.messageData, 'security') }}"
        {% endif %}
    </script>

    {{ parent() }}
{% endblock %}
